dependencies:
  espressif/cmake_utilities:
    version: "^1"
    rules: # will add "optional_component" only when all if clauses are True
      - if: "idf_version >=5.0"
      - if: "target in [esp32c2]"
  espressif/esp_matter: "^1.4.0" # SDK для Matter (Espressif)
  espressif/led_strip: "^3.0.1" # Драйвер WS2812 (RMT бэкенд)
  idf:
    version: ">=5.2.0"